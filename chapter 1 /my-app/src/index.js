import React from 'react'
import ReactDOM from 'react-dom/client'

import App from './App'

ReactDOM.createRoot(document.getElementById('root')).render(<App />)
// line above renders its contents into the div-element, defined in the file public/index.html having the id value root 