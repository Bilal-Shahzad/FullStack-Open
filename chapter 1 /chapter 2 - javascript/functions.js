const sum = (p1, p2) => {
    console.log(p1)
    console.log(p2)
    return p1 + p2
  }
//   defining an arrow function
const result = sum(1, 5)

//  single paramete exclude the parentheses 
const square = p => {
    return p * p
  }

//  further shorten the function
// const square = p => p * p